// not much to say here...just do it

#include <bits/stdc++.h>

using namespace std;

int main()
{
  int N;
  cin >> N;
  while (N--) {
    int G, P;
    cin >> G >> P;
    cout << G/(P+1) << endl;
  }

  return 0;
}
